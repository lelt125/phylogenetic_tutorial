# Normalize line endings to LF in the repository for all text files.
# Git will still check them out using your OS's native line endings
# if core.autocrlf is set, but the repo itself stays consistent.
* text=auto eol=lf

# Binary files - never touch line endings
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
